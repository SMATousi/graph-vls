defaults:
  - data: cora
  - train: full_graph
  - experiment: compression_sweep
  - _self_

wandb:
  project: graph-vls
  mode: offline

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  job:
    chdir: false
